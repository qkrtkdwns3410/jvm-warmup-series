# 3부. JIT는 어떤 코드를 컴파일할까

> 상태: 개요

## 다룰 내용

- 모든 코드를 즉시 컴파일하지 않는 이유
- invocation counter와 back-edge counter
- hot method와 hot loop
- 런타임 프로파일에 수집되는 정보
- 컴파일 임계값을 고정된 숫자로만 이해하면 안 되는 이유

## 독자가 가져갈 결론

JIT는 단순히 시간이 지나면 동작하는 것이 아니라 실제 실행 데이터를 바탕으로 최적화할 대상을 선택한다.

